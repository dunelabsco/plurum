[package]
name = "plurum-native-credential-store"
version = "0.0.0-development"
edition = "2021"
rust-version = "1.88"
license = "Apache-2.0"
publish = false
description = "Unwired Node-API boundary for Plurum's native credential store"
build = "build.rs"

[lib]
name = "plurum_native_credential_store"
crate-type = ["cdylib"]

[dependencies]
napi = { version = "=3.10.5", default-features = false, features = ["dyn-symbols", "napi8"] }
napi-derive = "=3.5.9"

[build-dependencies]
napi-build = "=2.3.2"

[lints.rust]
unsafe_code = "deny"
