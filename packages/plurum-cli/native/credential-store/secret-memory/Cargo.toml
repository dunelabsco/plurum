[package]
name = "plurum-native-secret-memory"
version = "0.0.0-development"
edition = "2021"
rust-version = "1.88"
license = "Apache-2.0"
publish = false
description = "Audited Node-API secret-memory boundary for Plurum"

[dependencies]
napi = { version = "=3.10.5", default-features = false, features = ["dyn-symbols", "napi8"] }

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
