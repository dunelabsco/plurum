# Environment variables
.env
.env.local
.env.*.local

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
dist/
build/
# but the openclaw plugin ships its built dist (clawhub fetches it from this subdir)
!plugins/openclaw/dist/
!plugins/openclaw/dist/**

# Node
node_modules/
.npm

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local config (contains API keys)
.mcp.json
.claude/

# Internal (kept local, not published) — root-anchored so they don't
# match same-named dirs deeper in the tree (e.g. web/src/app/docs).
/AGENTS.md
/.codex/
/CLAUDE.md
/docs/
/scripts/
logs.*.json

# OS
.DS_Store
Thumbs.db

# Build outputs
.next/
/out
/packages/plurum-cli/native/credential-store/target/

# Local research (cloned repos, not for commit)
research/

# Local-only seed-experience drafts (not part of the product)
experiences-drafts/
